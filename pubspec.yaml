name: pos_weight
description: "Pos weight Auto"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/POS.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/POS.png"
  min_sdk_android: 21