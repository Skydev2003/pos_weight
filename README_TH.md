# แอปพลิเคชันเครื่องชั่งดิจิทัล USB

แอปพลิเคชัน Flutter สำหรับอ่านค่าน้ำหนักจากเครื่องชั่งดิจิทัลผ่าน USB Serial แบบเรียลไทม์ พร้อมระบบตรวจจับอุปกรณ์อัตโนมัติและแสดงผลแบบ Smooth Animation

## ✨ คุณสมบัติหลัก

### 🔌 การเชื่อมต่ออัตโนมัติ
- **ตรวจจับอุปกรณ์อัตโนมัติ**: เสียบ USB แล้วเชื่อมต่อทันที ไม่ต้องกดปุ่ม
- **รองรับหลายรุ่น**: ทำงานได้กับเครื่องชั่งที่ใช้ USB Serial (CH340, FTDI, CP210x, etc.)
- **ตรวจจับการถอดสาย**: แจ้งเตือนทันทีเมื่อถอด USB

### 📊 การแสดงผลน้ำหนัก
- **แสดงผลแบบเรียลไทม์**: อัพเดทค่าน้ำหนักทันทีที่มีการเปลี่ยนแปลง
- **Smooth Animation**: ตัวเลขค่อยๆไหลขึ้นลงแบบนุ่มนวล เหมือนเครื่องชั่งจริง
- **ทศนิยม 3 ตำแหน่ง**: แสดงค่าน้ำหนักแม่นยำถึง 0.001 กิโลกรัม
- **UI สวยงาม**: ออกแบบด้วย Material Design 3 พร้อม Dark Theme

### ⚙️ การตั้งค่าอัตโนมัติ
- **ตรวจจับ Baud Rate**: ลองหา baud rate ที่เหมาะสมอัตโนมัติ (2400, 4800, 9600)
- **ตรวจจับ Data Format**: รองรับทั้ง 7E1 และ 8N1
- **จดจำการตั้งค่า**: บันทึกค่าที่ใช้ได้ครั้งล่าสุดเพื่อเชื่อมต่อเร็วขึ้น

### 🛡️ ความเสถียร
- **กรองข้อมูลเสีย**: ตรวจสอบและกรองข้อมูลที่ผิดพลาดออกอัตโนมัติ
- **จัดการ Error**: แสดงข้อความแจ้งเตือนที่เข้าใจง่ายเมื่อเกิดปัญหา
- **Reconnect อัตโนมัติ**: พยายามเชื่อมต่อใหม่เมื่อสัญญาณขาดหาย

## 🎯 การใช้งาน

### ขั้นตอนการใช้งาน

1. **เปิดแอป**: เปิดแอปพลิเคชันบนอุปกรณ์ Android
2. **เสียบ USB**: เชื่อมต่อเครื่องชั่งกับอุปกรณ์ผ่าน USB OTG
3. **อนุญาตสิทธิ์**: กดอนุญาตเมื่อมีข้อความขอสิทธิ์เข้าถึง USB
4. **เริ่มใช้งาน**: แอปจะเชื่อมต่อและแสดงค่าน้ำหนักอัตโนมัติ

### สถานะการเชื่อมต่อ

- 🔴 **Disconnected**: ยังไม่ได้เชื่อมต่อ USB
- 🟡 **Connecting**: กำลังเชื่อมต่อกับเครื่องชั่ง
- 🟢 **Connected**: เชื่อมต่อสำเร็จ พร้อมแสดงค่าน้ำหนัก
- 🔴 **Error**: เกิดข้อผิดพลาดในการเชื่อมต่อ

## 🔧 เทคโนโลยีที่ใช้

### Frontend (Flutter)
- **Flutter 3.x**: Framework หลักสำหรับพัฒนา UI
- **Riverpod**: State management แบบ reactive
- **Material Design 3**: ออกแบบ UI ที่ทันสมัย

### Backend (Android Native)
- **Kotlin**: ภาษาหลักสำหรับ Android
- **usb-serial-for-android**: ไลบรารีสำหรับสื่อสารกับ USB Serial
- **Platform Channels**: เชื่อมต่อระหว่าง Flutter และ Native Code

### การสื่อสาร
- **MethodChannel**: เรียกคำสั่งจาก Flutter ไป Native
- **EventChannel**: รับเหตุการณ์ USB แบบ Stream

## 📱 ความต้องการของระบบ

### อุปกรณ์
- Android 5.0 (API 21) ขึ้นไป
- รองรับ USB OTG
- เครื่องชั่งดิจิทัลที่ใช้ USB Serial

### เครื่องชั่งที่รองรับ
แอปรองรับเครื่องชั่งที่ใช้ USB Serial Chip ดังนี้:
- ✅ CH340/CH341 (VID: 0x1A86)
- ✅ FTDI FT232 (VID: 0x0403)
- ✅ CP210x (VID: 0x10C4)
- ✅ PL2303 (VID: 0x067B)
- ✅ CDC ACM (USB Standard)

### รูปแบบข้อมูลที่รองรับ
- **ASCII Text**: `"   0.480\n"`, `"0.480 kg"`, `"480 g"`
- **Binary**: 16-bit หรือ 32-bit integer/float
- **Baud Rate**: 2400, 4800, 9600 bps
- **Data Format**: 7E1, 8N1

## 🚀 การติดตั้งและพัฒนา

### ข้อกำหนดเบื้องต้น
```bash
# ติดตั้ง Flutter SDK
flutter --version  # ต้องเป็น 3.0 ขึ้นไป

# ติดตั้ง Android SDK
# Android Studio หรือ Android Command Line Tools
```

### การติดตั้ง Dependencies
```bash
# Clone โปรเจค
git clone <repository-url>
cd pos_weight

# ติดตั้ง dependencies
flutter pub get

# ตรวจสอบว่าพร้อมใช้งาน
flutter doctor
```

### การรันแอป
```bash
# เชื่อมต่ออุปกรณ์ Android หรือเปิด Emulator
flutter devices

# รันแอปในโหมด Debug
flutter run

# รันในโหมด Release
flutter run --release

# Build APK
flutter build apk --release
```

## 📂 โครงสร้างโปรเจค

```
pos_weight/
├── lib/
│   ├── main.dart                          # จุดเริ่มต้นของแอป
│   ├── models/
│   │   ├── connection_status.dart         # สถานะการเชื่อมต่อ
│   │   └── weight_reading.dart            # โมเดลข้อมูลน้ำหนัก
│   ├── providers/
│   │   └── scale_providers.dart           # Riverpod providers
│   ├── screens/
│   │   └── weight_monitor_screen.dart     # หน้าจอหลัก
│   └── services/
│       └── serial_scale_service.dart      # บริการสื่อสาร USB
├── android/
│   └── app/src/main/kotlin/
│       └── MainActivity.kt                # Native Android code
├── README.md                              # เอกสารภาษาอังกฤษ
└── README_TH.md                           # เอกสารภาษาไทย (ไฟล์นี้)
```

## 🎨 คุณสมบัติเด่น

### 1. Smooth Animation
ตัวเลขน้ำหนักจะค่อยๆเปลี่ยนแบบ animate ไม่กระโดด:
- ใช้ `AnimationController` และ `Tween`
- Curve: `easeOutCubic` เพื่อความนุ่มนวล
- ระยะเวลา: 400ms ต่อการเปลี่ยนค่า

### 2. การกรองข้อมูล
ตรวจสอบและกรองข้อมูลที่ผิดพลาด:
- ตรวจสอบรูปแบบตัวเลขด้วย Regex
- กรองตัวอักษรแปลกปลอม (noise)
- เลือกเฉพาะบรรทัดที่มีข้อมูลถูกต้อง

### 3. การจัดการ Timeout
ปรับ timeout ให้เหมาะสม:
- Read timeout: 200ms
- Total timeout: 800ms
- Idle timeout: 150ms

### 4. แคชการตั้งค่า
จดจำ config ที่ใช้ได้:
- บันทึก baud rate และ data format
- ลองใช้ config ที่รู้จักก่อน
- ลดเวลาในการเชื่อมต่อครั้งถัดไป

## 🐛 การแก้ปัญหา

### ปัญหา: แอปไม่เจอเครื่องชั่ง
**วิธีแก้:**
1. ตรวจสอบว่าสาย USB OTG ใช้งานได้
2. ลองถอดและเสียบใหม่
3. ตรวจสอบว่าเครื่องชั่งเปิดอยู่
4. กดปุ่ม "Retry Connection"

### ปัญหา: ค่าน้ำหนักไม่ถูกต้อง
**วิธีแก้:**
1. ตรวจสอบว่าเครื่องชั่งทำงานปกติ
2. ลองถอดและเสียบ USB ใหม่
3. ตรวจสอบ log ใน Logcat (tag: SerialScale)

### ปัญหา: ข้อมูลเสีย (corrupted)
**วิธีแก้:**
- แอปจะกรองข้อมูลเสียออกอัตโนมัติ
- ถ้ายังมีปัญหา ลองปรับ timeout ใน `MainActivity.kt`

### ปัญหา: เชื่อมต่อช้า
**วิธีแก้:**
- แอปจะจดจำ config ที่ใช้ได้
- ครั้งถัดไปจะเชื่อมต่อเร็วขึ้น
- ถ้ายังช้า ลองลด delay ใน `openSerialPort()`

## 📝 การปรับแต่ง

### เปลี่ยน Baud Rate
แก้ไขใน `MainActivity.kt`:
```kotlin
private val serialConfigs = listOf(
    SerialConfig("9600 8N1", 9600, ...),  // เพิ่ม baud rate ที่ต้องการ
    SerialConfig("19200 8N1", 19200, ...), // ตัวอย่าง
)
```

### เปลี่ยนความเร็ว Animation
แก้ไขใน `weight_monitor_screen.dart`:
```dart
_controller = AnimationController(
  duration: const Duration(milliseconds: 400), // เปลี่ยนเป็น 200-600ms
  vsync: this,
);
```

### เปลี่ยนจำนวนทศนิยม
แก้ไขใน `weight_monitor_screen.dart`:
```dart
final weightText = _displayedWeight.toStringAsFixed(3); // เปลี่ยนเป็น 1-4
```

## 🔐 สิทธิ์ที่ใช้

แอปต้องการสิทธิ์ดังนี้:
- `android.hardware.usb.host`: เข้าถึง USB Host
- `android.permission.USB_PERMISSION`: ใช้งาน USB device

## 📄 License

โปรเจคนี้เป็น Open Source สามารถนำไปใช้และแก้ไขได้ตามต้องการ

## 👨‍💻 ผู้พัฒนา

พัฒนาด้วย Flutter และ Kotlin สำหรับระบบ POS และเครื่องชั่งดิจิทัล

## 🙏 ขอบคุณ

- [usb-serial-for-android](https://github.com/mik3y/usb-serial-for-android) - ไลบรารี USB Serial
- [Flutter](https://flutter.dev) - Framework
- [Riverpod](https://riverpod.dev) - State Management

## 📞 ติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. เปิด Issue ใน GitHub
2. ตรวจสอบ log ใน Logcat (tag: SerialScale)
3. แนบข้อมูลเครื่องชั่งที่ใช้ (VID/PID)

---

**หมายเหตุ**: แอปนี้ออกแบบมาสำหรับเครื่องชั่งที่ส่งข้อมูลแบบ continuous stream ถ้าเครื่องชั่งของคุณต้องส่งคำสั่งก่อน อาจต้องปรับแต่งโค้ดเพิ่มเติม
